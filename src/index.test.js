import {
  renderLists,
  renderTaskCount,
  colorTasks,
  openOrCloseAddTaskForm,
  openOrCloseUpdateTaskForm,
  renderTasks,
  render,
  renderAndSave,
  editTask,
  createList,
  createTask,
  removeTask,
  closeModal,
} from './index';

describe('createList', () => {
  test('should return a function', () => {
    expect(typeof createList).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof createList).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof createList).not.toBe('class');
  });
});

describe('createTask', () => {
  test('should return a function', () => {
    expect(typeof createTask).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof createTask).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof createTask).not.toBe('class');
  });
});

describe('editTask', () => {
  test('should return a function', () => {
    expect(typeof editTask).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof editTask).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof editTask).not.toBe('class');
  });
});

describe('removeTask', () => {
  test('should return a function', () => {
    expect(typeof removeTask).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof removeTask).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof removeTask).not.toBe('class');
  });
});

describe('render', () => {
  test('should return a function', () => {
    expect(typeof render).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof render).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof render).not.toBe('class');
  });
});

describe('renderAndSave', () => {
  test('should return a function', () => {
    expect(typeof renderAndSave).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof renderAndSave).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof renderAndSave).not.toBe('class');
  });
});

describe('renderTasks', () => {
  test('should return a function', () => {
    expect(typeof renderTasks).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof renderTasks).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof renderTasks).not.toBe('class');
  });
});

describe('closeModal', () => {
  test('should return a function', () => {
    expect(typeof closeModal).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof closeModal).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof closeModal).not.toBe('class');
  });
});

describe('renderLists', () => {
  test('should return a function', () => {
    expect(typeof renderLists).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof renderLists).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof closeModal).not.toBe('class');
  });
});

describe('renderTaskCount', () => {
  test('should return a function', () => {
    expect(typeof renderTaskCount).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof renderTaskCount).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof renderTaskCount).not.toBe('class');
  });
});

describe('openOrCloseAddTaskForm', () => {
  test('should return a function', () => {
    expect(typeof openOrCloseAddTaskForm).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof openOrCloseAddTaskForm).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof openOrCloseAddTaskForm).not.toBe('class');
  });
});

describe('colorTasks', () => {
  test('should return a function', () => {
    expect(typeof colorTasks).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof colorTasks).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof colorTasks).not.toBe('class');
  });
});

describe('openOrCloseUpdateTaskForm', () => {
  test('should return a function', () => {
    expect(typeof openOrCloseUpdateTaskForm).toBe('function');
  });

  test('should not be array', () => {
    expect(typeof openOrCloseUpdateTaskForm).not.toBe('array');
  });

  test('should not be class', () => {
    expect(typeof openOrCloseUpdateTaskForm).not.toBe('class');
  });
});